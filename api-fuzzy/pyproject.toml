[project]
name = "api-fuzzy"
version = "0.1.0"
description = "Fuzzy matching API service"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "pymssql",
    "rapidfuzz"
]
